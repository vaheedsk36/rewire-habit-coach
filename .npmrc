# Flat node_modules (npm/yarn-like). Required for bare React Native: native
# autolinking (CocoaPods / Gradle) and Metro resolve modules from a flat tree,
# not pnpm's default symlinked/isolated layout. Applies to the whole workspace;
# the Next.js web app builds fine either way.
node-linker=hoisted

// @rewire/core — framework-agnostic code shared by the web (Next.js) and mobile
// (Expo) apps: Zod schemas, inferred types, prompt builders, and pure helpers.
// Populated in the next step; kept as a valid module so the workspace resolves.
export const CORE_VERSION = "0.1.0";
